from movieData import MovieData

movie_title = 'black panther'
full_plot = False
load_all = True
movie = MovieData(movie_title, full_plot, load_all)

print(movie.trivia)

for key, value in movie.movie_technical_info.items():
    print(key.title() + ":", value)
